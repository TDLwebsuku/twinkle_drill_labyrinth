<% @cart_items.each do |cart_item| %>
  <strong>商品名:</strong><%= cart_item.item.item_name %>
  <strong>数量:</strong><%= cart_item.count %>枚
  <strong>金額:</strong><%= cart_item.item.price %>円
<% end %>
<strong>合計金額：<%= total_price %>円</strong>
<%= form_tag('/orders', method: 'post') do %>
  <div class="field">
    <p>郵便番号</p>
    <%= text_field_tag :zip_code %>
  </div>

  <div class="field">
    <p>住所</p>
    <%= text_field_tag :address %>
  </div>

  <div class="field">
    <p>phone_number</p>
    <%= text_field_tag :phone_number %>
  </div>

  <div class="field">
    <p>last_name</p>
    <%= text_field_tag :last_name, current_user.last_name %>
  </div>

  <div class="field">
    <p>first_name</p>
    <%= text_field_tag :first_name, current_user.first_name %>
  </div>

  <div class="field">
    <p>last_name_kana</p>
    <%= text_field_tag :last_name_kana, current_user.last_name_kana %>
  </div>

  <div class="field">
    <p>first_name_kana</p>
    <%= text_field_tag :first_name_kana, current_user.first_name_kana %>
  </div>
    <%= submit_tag "購入画面に進む" %>
<% end %>
