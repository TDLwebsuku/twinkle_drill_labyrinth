<%= form_with(model: item, local: true) do |f| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_name %>
    <%= f.text_field :item_name, id: :item_item_name %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price, id: :item_price %>円
  </div>

  <div class="field">
    <%= f.label :artist_name %>
    <%= f.text_field :artist_name, id: :item_artist_name %>
  </div>

  <div class="field">
    <%= f.label :item_image %>
    <%= f.attachment_field :item_image %>
  </div>

  <div class="field">
    <%= f.label :category %>
    <%= f.select :category, Item.categories.keys.to_a %>
  </div>

  <div class="field">
    <%= f.label :label %>
    <%= f.text_field :label, id: :item_label %>
  </div>

  <div class="field">
    <%= f.label :stock %>
    <%= f.number_field :stock, id: :item_stock %>
  </div>

  <div class="field">
    <%= f.label :is_available %>
    <%= f.select :is_available, Item.is_availables.keys.to_a %>
  </div>

    <%= f.hidden_field :admin_id, :value => current_admin.id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
